import AssistenteDashboard from './AssistenteDashboard';

// Reuse the same component as it already handles all pedidos listing
const AdminPedidosPage = () => {
  return <AssistenteDashboard />;
};

export default AdminPedidosPage;
