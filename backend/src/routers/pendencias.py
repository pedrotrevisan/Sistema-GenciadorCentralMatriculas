"""Router de Pendências - Placeholder para futura refatoração"""
from fastapi import APIRouter

# Este router será implementado quando movermos as rotas de pendências do server.py
# Por enquanto, mantemos as rotas no server.py para minimizar riscos

router = APIRouter(tags=["Pendências"])

# TODO: Mover rotas de /pendencias do server.py para cá
